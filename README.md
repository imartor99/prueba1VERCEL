# 📚 The Reading List

Este proyecto es una aplicación web simple para gestionar una lista de lectura, implementada con JavaScript vanilla y clases ES6, siguiendo el patrón modular (Modelo-Vista-Controlador).

## 📄 Estructura del Proyecto

- `index.html`: La estructura principal de la aplicación.
- `css/style.css`: Estilos de la aplicación.
- `js/`: Contiene la lógica modularizada:
    - `book.js`: Clase que representa un libro (Modelo).
    - `booklist.js`: Clase que gestiona la colección de libros (Modelo/Controlador).
    - `main6.js`: Inicializa la aplicación y maneja los eventos del DOM (Controlador/Vista).
- `tests/`: Contendrá los archivos de pruebas unitarias.

---

## 🚀 Despliegue CI/CD Automatizado

La aplicación utiliza un flujo de **Integración Continua / Despliegue Continuo (CI/CD)** en GitHub Actions para asegurar la calidad del código y automatizar la entrega de la documentación técnica.

*(Esta sección será completada tras el despliegue final)*